<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Mlm" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Mlm" parent="Base.Theme.Mlm" >
        <item name="android:statusBarColor">@color/surface_card_normal_default</item>
        <item name="android:navigationBarColor">@color/surface_card_normal_default</item>
    </style>
</resources>